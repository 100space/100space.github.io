export interface IconProps {
    pic?: string
}
export interface ProjectProps {
    subject: string
    date: string
    contributor: string
    roles: string
    URL: string
    explain: string
    skill: string[]
}
